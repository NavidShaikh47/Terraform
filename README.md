These are the working files only
You need to run comands to intialize terraform working directory
terraform init
terraform validate 
terraform plan
terraform apply
terraform destroy (to delete infra )
